<%- include("./partials/header") %>
<%- include("./partials/navbar") %>

<div class="container mt-5">
    <h3>Your Published Blogs 😇</h3>

    <% blogs.reverse() %>
    <% blogs.forEach(blog => { %>
      <div class="list">
        <div class="card mt-3">
          <div class="card-body">
            <p class="text-muted">Published at: <%= blog.posted_at %></p>
            <h5 class="card-title"><%= blog.title %></h5>
            <p class="card-text"><%= blog.content.slice(0, 99) %>... <a href="/blog/more/<%= blog._id %>">Read More</a></p>
            <a href="/blog/more/<%= blog._id %>" class="btn btn-sm btn-primary">Read More...</a>
            <a href="/blog/delete/<%= blog._id %>" class="btn btn-sm btn-danger mx-1">Delete</a>
          </div>
        </div>
      </div>
    <%}) %>

    <a href="/draft" class="btn btn-sm my-5" style="background-color:antiquewhite ;">View Your Drafts</a>
    
</div>

<div>
  <ul class="listPage">
  
  </ul>
</div>

<script src="index.js"></script>

<%- include("./partials/footer") %>